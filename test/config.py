import os
"""Configuration file for all constants that we will use inside project
Add you'r constants if needed."""

users_path = os.path.join("data", "users.json")
posts_path = os.path.join("data", "notes.json")
